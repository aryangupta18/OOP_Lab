1. complex class ( two variables int and float)
2. illustrate concept of destructor.
    - use func that pass obj
    - organize your code in main
    - destructor can have print statement that the destructor is called

3. wap to showcase the imp of destructor in case of dma
4. wap where global variable is inc and dec when a const or destruc is called 

5. wap where a cons of class takes an int argumet now declare an array of obj
    - there is a public member func printVal() that prints the int. write a for loop for objects to call tha func
    - study cons and destruc call for dma (single obj and array of objects)
    - how can a user definrd object be created

6. composition in class
    - author class - which contains author name and birthyear
    - book class - which contains author as member feild, booktitle, publisher and the year as the data feilds
    - create constructor and destructor for both 
    - see the order in which they are called

7. 