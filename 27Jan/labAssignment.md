**27/01/26**
function using reference variables
what are reference variables
write a func to cal quotient and remainder and return both
default parameters
    1. how do they operate
    2. skip some for right
    3. how is the func call affected if we are specifying one or two or more
    4. can we skip specifying parameters in the func while calling 
    take volume func with 3 parameters
function prototype and definition
    if we design prototype for volume func (int,int,int=2) like this and while writing definition if we change volume(inta,intb,intc=5). which value prevails

function overloading
    1. can return types be different
        use a square function to square int, float, and double.
        a. same parameters and same return type
        b. same parameters but different return types
    2. what happens if you combine overloading with default parameters
